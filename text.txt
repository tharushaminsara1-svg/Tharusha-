<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ariya's Loku Tharuwa · God of Server</title>
    <!-- Font Awesome 6 (free) for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', 'Fira Code', 'Courier New', monospace;
        }

        body {
            background-color: #0b0f1c;
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(10, 230, 150, 0.03) 0%, transparent 30%),
                repeating-linear-gradient(45deg, rgba(0, 255, 200, 0.02) 0px, rgba(0, 255, 200, 0.02) 2px, transparent 2px, transparent 8px);
            color: #b0ffdd;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
            position: relative;
            overflow-x: hidden;
        }

        /* CRT flicker + scanlines */
        body::before {
            content: "";
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: repeating-linear-gradient(0deg, rgba(0, 20, 10, 0.1) 0px, rgba(0, 255, 200, 0.02) 2px, transparent 3px, transparent 5px);
            pointer-events: none;
            z-index: 5;
            animation: scan 12s linear infinite;
        }

        @keyframes scan {
            0% { background-position: 0 0; }
            100% { background-position: 0 15px; }
        }

        /* main terminal card */
        .terminal {
            background-color: #0e121f;
            border: 2px solid #1f3b36;
            border-radius: 28px;
            box-shadow: 0 0 30px #00ffc480, inset 0 0 10px #00cc9940;
            max-width: 1000px;
            width: 100%;
            padding: 2.2rem 2rem;
            backdrop-filter: blur(2px);
            position: relative;
            z-index: 10;
        }

        /* header with glitch effect */
        .glitch-box {
            display: flex;
            flex-wrap: wrap;
            align-items: baseline;
            justify-content: space-between;
            border-bottom: 2px solid #2c5a4e;
            padding-bottom: 1.2rem;
            margin-bottom: 2rem;
        }

        .identity {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            gap: 1rem 0.8rem;
        }

        .name-badge {
            background: #0d1f1b;
            padding: 0.4rem 1.4rem 0.4rem 1.8rem;
            border-radius: 40px;
            border: 1px solid #00ffbb;
            box-shadow: 0 0 15px #00ffaa70;
            font-size: 2rem;
            font-weight: 700;
            letter-spacing: 2px;
            color: #e6fffa;
            text-transform: uppercase;
            position: relative;
        }

        .name-badge::before {
            content = "";
            position: absolute;
            left: 10px; top: 25%;
            height: 50%;
            width: 5px;
            background: #00ffc8;
            filter: blur(2px);
        }

        .sri-lanka {
            display: flex;
            align-items: center;
            gap: 8px;
            background: #102a24;
            padding: 0.3rem 1rem 0.3rem 1rem;
            border-radius: 40px;
            border: 1px solid #32a585;
            font-size: 1.25rem;
            font-weight: 500;
        }
        .sri-lanka i {
            font-size: 1.8rem;
            color: #ffd966;
        }

        .whatsapp-badge {
            background: #0e2d21;
            border: 1px solid #25D366;
            border-radius: 40px;
            padding: 0.4rem 1.2rem;
            font-size: 1.35rem;
            font-weight: 600;
            color: #c6ffb3;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .whatsapp-badge i {
            color: #25D366;
            font-size: 1.7rem;
            filter: drop-shadow(0 0 6px #25D366);
        }

        .whatsapp-badge span {
            font-family: 'Courier New', monospace;
            letter-spacing: 2px;
        }

        /* alias – god of server */
        .title-god {
            text-align: right;
            margin-top: 0.6rem;
            font-size: 2rem;
            text-transform: uppercase;
            font-weight: 800;
            color: transparent;
            -webkit-text-stroke: 2px #5affcf;
            text-stroke: 2px #5affcf;
            letter-spacing: 6px;
            opacity: 0.7;
            animation: flicker 4s infinite;
        }
        @keyframes flicker {
            0% { opacity: 0.7; text-shadow: 0 0 5px #00ffe0; }
            50% { opacity: 1; text-shadow: 0 0 20px #00ffc8, 0 0 5px #80ffdb; }
            100% { opacity: 0.7; text-shadow: 0 0 5px #00ffe0; }
        }

        /* main stats / hacking vibe */
        .matrix-panel {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.8rem;
            margin: 2rem 0 1.5rem;
        }

        @media (max-width: 650px) {
            .matrix-panel {
                grid-template-columns: 1fr;
            }
        }

        .card {
            background: #0a161e;
            border: 1px solid #266650;
            border-radius: 24px;
            padding: 1.5rem 1.8rem;
            box-shadow: inset 0 0 20px #000c10, 0 8px 0 #0b2f26;
        }

        .card h3 {
            font-size: 1.3rem;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 1.2rem;
            color: #8fffcb;
            border-left: 6px solid #00ffb3;
            padding-left: 0.8rem;
        }

        .server-metrics {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .metric-item {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }
        .metric-item i {
            width: 30px;
            font-size: 1.6rem;
            color: #34d8a0;
        }
        .metric-bar {
            flex: 1;
            height: 14px;
            background-color: #1c2d30;
            border-radius: 20px;
            border: 1px solid #00a381;
        }
        .metric-fill {
            height: 100%;
            width: 0%;
            background: linear-gradient(90deg, #00c49a, #bcffdb);
            border-radius: 20px;
            box-shadow: 0 0 10px #00ffb3;
        }

        /* attacker's log / live report */
        .log-console {
            background: #0b121b;
            border-radius: 20px;
            padding: 1rem 0 0.5rem 0;
            border: 1px solid #2d745e;
            font-family: 'Courier New', monospace;
        }

        .log-line {
            padding: 0.25rem 1.2rem;
            font-size: 0.95rem;
            border-bottom: 1px solid #1d493b;
            color: #b3ffe0;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        .log-line i {
            width: 22px;
            color: #60ffb5;
            margin-right: 8px;
        }
        .log-line .highlight {
            color: #f0ff60;
            font-weight: 600;
        }
        .blink-cursor {
            display: inline-block;
            width: 12px;
            height: 1.2rem;
            background-color: #00ffc3;
            vertical-align: middle;
            margin-left: 8px;
            animation: blink 1s step-end infinite;
            box-shadow: 0 0 10px #00ffd9;
        }
        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }

        /* whatsapp + report area */
        .contact-panel {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            align-items: center;
            background: #0a231e;
            border-radius: 60px;
            padding: 1rem 2rem 1rem 2rem;
            margin: 2rem 0 1rem;
            border: 1px solid #31b191;
        }

        .wa-large {
            display: flex;
            align-items: center;
            gap: 15px;
            font-size: 2.1rem;
            font-weight: 600;
        }
        .wa-large i {
            font-size: 3rem;
            color: #25D366;
            filter: drop-shadow(0 0 8px #00ffb3);
        }
        .wa-large span {
            font-family: 'Courier New', monospace;
            background: #102f28;
            padding: 0.2rem 1.2rem;
            border-radius: 40px;
            border: 1px solid #41c9a4;
            letter-spacing: 3px;
        }

        .report-btn {
            background: #0f2922;
            border: 2px solid #2dc9a5;
            color: #d5ffec;
            padding: 0.8rem 2rem;
            border-radius: 40px;
            font-size: 1.4rem;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 10px;
            cursor: pointer;
            transition: 0.2s;
            box-shadow: 0 0 15px #14cc9e;
        }
        .report-btn i {
            font-size: 1.8rem;
        }
        .report-btn:hover {
            background: #1f4f41;
            box-shadow: 0 0 30px #6effd5;
            border-color: #aaffdd;
            transform: scale(1.02);
        }

        /* country mention +94 */
        .flag-row {
            display: flex;
            align-items: center;
            gap: 20px;
            margin: 1.5rem 0 1rem;
            color: #b6ffe6;
            font-size: 1.3rem;
        }
        .flag-icon {
            font-size: 2.5rem;
            filter: drop-shadow(0 0 6px gold);
        }
        .code-badge {
            background: #1c3d30;
            padding: 0.4rem 2rem;
            border-radius: 30px;
            border: 1px solid #64c9a8;
            font-weight: 600;
        }

        /* server status footer */
        .god-signature {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            margin-top: 2rem;
            gap: 1rem;
            color: #55bbaa;
            border-top: 1px dashed #2d6d58;
            padding-top: 1.5rem;
            font-size: 1.1rem;
        }
        .god-signature i {
            font-size: 2rem;
            color: #d4ffb0;
        }

        /* fake terminal blink */
        .cursor-em {
            display: inline-block;
            width: 8px;
            background: #00ff99;
            margin-left: 5px;
        }
    </style>
</head>
<body>
    <div class="terminal">

        <!-- header with name & country +94 -->
        <div class="glitch-box">
            <div class="identity">
                <div class="name-badge">ARIYA'S LOKU THARUWA</div>
                <div class="sri-lanka">
                    <i class="fas fa-flag-checkered flag-icon"></i> <!-- lanka flag vibe -->
                    <span>SRI LANKA <span style="font-weight:300; margin-left: 6px;">+94</span></span>
                </div>
            </div>
            <div class="whatsapp-badge">
                <i class="fab fa-whatsapp"></i>
                <span>+94 77 123 4567</span> <!-- whatsapp report number -->
            </div>
        </div>

        <!-- god of server title -->
        <div class="title-god">⚡ GOD OF SERVER ⚡</div>

        <!-- main hacking metrics / server info -->
        <div class="matrix-panel">
            <!-- left card: webdev / hacker stats -->
            <div class="card">
                <h3><i class="fas fa-skull" style="margin-right: 8px;"></i> ROOT ACCESS</h3>
                <div class="server-metrics">
                    <div class="metric-item">
                        <i class="fas fa-microchip"></i>
                        <span>CPU overload</span>
                        <div class="metric-bar"><div class="metric-fill" style="width:98%"></div></div>
                        <span>98%</span>
                    </div>
                    <div class="metric-item">
                        <i class="fas fa-memory"></i>
                        <span>MEM spray</span>
                        <div class="metric-bar"><div class="metric-fill" style="width:87%"></div></div>
                        <span>87%</span>
                    </div>
                    <div class="metric-item">
                        <i class="fas fa-database"></i>
                        <span>SQL inj.</span>
                        <div class="metric-bar"><div class="metric-fill" style="width:73%"></div></div>
                        <span>73%</span>
                    </div>
                    <div class="metric-item">
                        <i class="fas fa-network-wired"></i>
                        <span>PACKET LOSS</span>
                        <div class="metric-bar"><div class="metric-fill" style="width:42%"></div></div>
                        <span>42%</span>
                    </div>
                </div>
                <div style="margin-top: 1.8rem; background: #00000030; padding: 0.7rem; border-radius: 14px; border-left: 4px solid #31c7a0;">
                    <i class="fas fa-terminal" style="margin-right: 8px;"></i> 
                    <span style="font-family: monospace;">root@ariya:~# ./exploit --target lanka_net</span>
                </div>
            </div>

            <!-- right card: log / report simulation -->
            <div class="card">
                <h3><i class="fas fa-terminal"></i> LIVE REPORT · WHATSAPP LOG</h3>
                <div class="log-console">
                    <div class="log-line"><i class="fas fa-check-circle"></i> [<span class="highlight">+94</span>] inbound request from colombo </div>
                    <div class="log-line"><i class="fas fa-shield-hal"></i> bypass WAF on .lk domain</div>
                    <div class="log-line"><i class="fas fa-skull"></i> root@server ~ # <span class="highlight">whoami</span> → god_of_server</div>
                    <div class="log-line"><i class="fas fa-whatsapp"></i> msg to +94 77 123 4567 : "report #LKA23"</div>
                    <div class="log-line"><i class="fas fa-bolt"></i> DDoS mitigation active ...</div>
                    <div class="log-line" style="border-bottom: none;">
                        <i class="fas fa-circle" style="font-size: 0.5rem; color: #00ffb3;"></i> 
                        waiting for command <span class="blink-cursor"></span>
                    </div>
                </div>
                <!-- tiny note: webdeveloper signature -->
                <div style="margin-top: 1.2rem; text-align: right; color: #6ea;">
                    <i class="fas fa-code"></i> webdeveloper / ethical hacking
                </div>
            </div>
        </div>

        <!-- WhatsApp report number + country big emphasis -->
        <div class="contact-panel">
            <div class="wa-large">
                <i class="fab fa-whatsapp"></i>
                <span>+94 77 123 4567</span>
            </div>
            <div class="report-btn" id="reportTrigger">
                <i class="fas fa-bug"></i> REPORT <span style="font-size:1rem;">(simulate)</span>
            </div>
        </div>

        <!-- country +94 line again and "god of server" -->
        <div class="flag-row">
            <i class="fas fa-flag fa-2x" style="color: #ffd966;"></i>
            <span class="code-badge"><i class="fas fa-map-pin"></i> SRI LANKA · +94 · whatsapp red team</span>
            <span style="flex:1; text-align: right; font-size: 1.5rem; letter-spacing: 4px;">💀</span>
        </div>

        <!-- footer signature with tem name -->
        <div class="god-signature">
            <i class="fas fa-crown"></i>
            <span>#GOD_OF_SERVER · root access granted · ariyas loku tharuwa</span>
            <i class="fas fa-server"></i>
        </div>
    </div>

    <!-- hidden interaction for report button + js -->
    <script>
        (function() {
            const reportBtn = document.getElementById('reportTrigger');
            // whatsapp number variable (you can edit in code)
            const waNumber = "+94 77 123 4567";   // exactly as shown

            // create subtle alert/toast simulation (hacking style)
            function showFakeReport() {
                // build a floating console line
                const fakeDiv = document.createElement('div');
                fakeDiv.style.position = 'fixed';
                fakeDiv.style.bottom = '25px';
                fakeDiv.style.right = '25px';
                fakeDiv.style.background = '#0b211c';
                fakeDiv.style.border = '2px solid #1ed99a';
                fakeDiv.style.color = '#c6ffdd';
                fakeDiv.style.padding = '1rem 2rem';
                fakeDiv.style.borderRadius = '50px';
                fakeDiv.style.fontFamily = "'Courier New', monospace";
                fakeDiv.style.boxShadow = '0 0 40px #00ffc8';
                fakeDiv.style.zIndex = '9999';
                fakeDiv.style.backdropFilter = 'blur(4px)';
                fakeDiv.style.fontSize = '1.3rem';
                fakeDiv.style.letterSpacing = '1px';
                fakeDiv.innerHTML = `<i class="fas fa-whatsapp" style="color:#25D366; margin-right:12px;"></i> 📡 REPORT FORWARDED TO ${waNumber} (simulated)`;
                document.body.appendChild(fakeDiv);

                // add a secondary line (like sri lanka)
                const note = document.createElement('div');
                note.style.marginTop = '8px';
                note.style.fontSize = '1rem';
                note.style.color = '#9fe6cf';
                note.innerHTML = '✦ God of server acknowledges +94 report ✦';
                fakeDiv.appendChild(note);

                // remove after 4 seconds
                setTimeout(() => {
                    fakeDiv.style.transition = 'opacity 0.6s';
                    fakeDiv.style.opacity = '0';
                    setTimeout(() => {
                        if (fakeDiv.parentNode) fakeDiv.remove();
                    }, 700);
                }, 3800);
            }

            reportBtn.addEventListener('click', (e) => {
                e.preventDefault();
                showFakeReport();

                // also add a small glitch effect on terminal (visual)
                document.querySelector('.terminal').style.transition = 'box-shadow 0.1s';
                document.querySelector('.terminal').style.boxShadow = '0 0 80px #59ffcf, 0 0 30px #00ffb3';
                setTimeout(() => {
                    document.querySelector('.terminal').style.boxShadow = '0 0 30px #00ffc4, inset 0 0 10px #00cc99';
                }, 200);
            });

            // dynamic year or something? just for fun: update log line (optional)
            const logLines = document.querySelectorAll('.log-line');
            // replace a line to show report number (already done)
            // extra: simulate second live change
            setInterval(() => {
                const possibleLine = logLines[2];
                if (possibleLine) {
                    const oldHTML = possibleLine.innerHTML;
                    // randomly change icon? minimal — keep fresh
                }
            }, 5000);
        })();
    </script>

    <!-- small note to ensure +94, name, wa report number, god of server appears as text -->
    <!-- additional hidden watermark (just for reference) -->
    <div style="display: none;">ariyas loku tharuwa, whatsapp report +94 77 123 4567, sri lanka, god of server</div>
</body>
</html>